---
description: SSOT of all active validation harnesses, hooks, and gatekeepers to ensure agent execution is deterministic, database targets are correct, and code commits conform.
globs: *
alwaysApply: true
---

# Active Harnesses & Validation Gates

This document is automatically generated by `sync_hooks_to_rules.py` from active python harnesses and hooks. Do not edit this file directly.

## `perplexity-inbox/harness/agentic_checks.py`
Deterministic Validation Harness for Agentic Workflows.

Implements objective, rule-based verification checks as actionable pathways
(tools) that agents can call programmatically to verify system state.

### `class DeterministicHarness`
#### Methods:
- `def __init__(self, workspace_root)`
- `def check_git_isolation(self)`
  Pathway 1: Verify that the agent is running in a configured git worktree or isolated branch.
- `def check_database_capabilities(self, uri)`
  Pathway 2: Verify PostgreSQL capabilities, active extensions, and graph namespaces.
- `def check_vector_dimension_match(self, schema_file, expected_dim)`
  Pathway 3: Verify that SQL schema vector columns match the expected embeddings dimension size.
- `def verify_no_vibe_self_ratings(self, file_path)`
  Pathway 4: Ensures that validation files do not rely on subjective, unverified self-ratings.


### Functions
- `def print_help()`
- `def main()`

---

## `perplexity-inbox/harness/auto_post_status.py`
### Functions
- `def get_active_conv_id()`
- `def make_token(sheet_id, role)`
- `def api_post(path, body, token)`
- `def run_cmd(cmd, cwd)`
- `def main()`

---

## `perplexity-inbox/harness/db_harness.py`
Database target harness connection guards (Rod 1 & No Silent Failures).

Deterministic boundary validator ensuring that AI agents and scripts connect 
to the canonical databases (e.g., amplified_brain) and preventing split-database
pollution of the orchestrator/metadata databases (e.g., cove).

### Functions
- `def parse_db_name(connection_uri)`
  Safely extracts the database name from a connection URI.
- `def assert_db_target(connection_uri, expected_db, min_graph_nodes)`
  Asserts that the connection points to the correct canonical database
  
  and verifies that the database contains the expected production graphs
  and minimum node count. Raises AssertionError on mismatch.

---

## `perplexity-inbox/harness/ewans_mouth.py`
Ewan's Mouth Intake & Annotation Parser.

Processes Ewan's verbatim raw inputs and produces annotated outputs marking:
  - [CERTAIN]: Clear, literal factual constraints or direct rules.
  - [AMBIGUOUS]: Vague phrases requiring gatekeeping clarification.
  - [METAPHOR]: Non-literal structural mental models (e.g. triangles, templates).
  - [IMPRECISE]: colloquial technical references needing alignment with ESTATE-TAXONOMY.md.

Designed to exploit Anthropic prompt caching by placing the static database
and architecture context first, appending the dynamic verbatim message at the end.

### `class EwansMouthParser`
#### Methods:
- `def __init__(self, taxonomy_file)`
- `def build_cached_prompt(self, user_message)`
  Assembles the prompt. The system instructions and taxonomy are placed
  
  first to leverage prompt caching, while the user text is appended.
- `def simulate_parse(self, text)`
  Offline mock parser displaying example annotations based on common patterns.


---

## `perplexity-inbox/harness/gatekeeper.py`
Workspace and Database Gatekeeper.

Executes objective verification checks on git branch, PostgreSQL targets,
database extensions, and implementation plan frontmatter.
Returns exit code 0 on success, or exit code 1 if checks fail.

### Functions
- `def run_checks()`

---

## `perplexity-inbox/harness/hooks/antigravity-post-write-shape-gate.py`
Antigravity post-write adapter — shape gate (stdin or --path).

### Functions
- `def main()`

---

## `perplexity-inbox/harness/hooks/claude-post-write-shape-gate.py`
Claude Code PostToolUse adapter — shape gate on Write|Edit.

### Functions
- `def main()`

---

## `perplexity-inbox/harness/hooks/cursor-after-file-edit-shape-gate.py`
Cursor afterFileEdit adapter — shape gate on Write.

### Functions
- `def main()`

---

## `perplexity-inbox/harness/hooks/shape-gate-cli.py`
CLI — run shape gate on a file (Antigravity verification loop + manual bee).

### Functions
- `def main()`

---

## `perplexity-inbox/harness/shape_gate.py`
Shape gate — deterministic tier + attribution check at boundaries (Rod 1).

Non-judgmental: checks shape (tier arithmetic, required fields), not semantic truth.
Human and agent emissions use the same OPA policy; ceilings differ only by promotion path.

SSOT: perplexity-inbox/harness/

### Functions
- `def ssot_root()`
- `def opa_bundle_candidates()`
- `def resolve_opa_bundle()`
- `def witness(hook, event, ok, detail)`
- `def extract_frontmatter(content)`
- `def classify_actor(author, actors)`
- `def infer_author(fm, seat)`
- `def build_opa_input(fm, path, seat)`
- `def opa_check(opa_input)`
- `def check_content(content, path, seat)`
- `def check_file(path, seat)`
- `def emit_verdict(denies, path, fm, seat)`

---

## `perplexity-inbox/harness/sync_ledger_to_dbs.py`
Deterministic Prompt and Rules Sync Pipeline.

Propagates updates from the filesystem ledger (master) to the cove and
amplified_brain databases, enforcing structural discipline and preventing drift.

### Functions
- `def get_file_sha256(filepath)`
  Computes SHA-256 hash of the entire file on disk.
- `def escape_sql_val(val)`
  Escapes single quotes for SQL/Cypher statements.
- `def parse_and_validate_ledger_file(filepath)`
  Parses Dual-YAML frontmatter and body, asserting the 19 required fields.
- `def sync_prompts(prompts_dir, force)`
  Syncs prompt ledger files to cove.system_prompts database table.
- `def sync_logic_packets(packets_dir, force)`
  Syncs logic packets ledger files to amplified_brain Apache AGE graph.
- `def verify_drift(prompts_dir, packets_dir)`
  Verifies alignment between filesystem and databases, returning non-zero on drift.
- `def main()`

---

## `perplexity-inbox/harness/term_gate.py`
Terminology Alignment Gate (AI-Facing Disambiguation Algorithm).

Translates Ewan's imprecise human terminology into precise database/code targets,
resolving ambiguities by posing non-technical, context-based questions to determine
intent without exposing raw database names or technical terminology to the user.

### Functions
- `def analyze_text(text)`
  Scans instruction text for taxonomy matches.
- `def check_for_ambiguity(text)`
  Detects dangerous or ambiguous naming overlaps in instructions.
- `def get_context_diagnostic()`
  Returns the sequence of non-technical context questions to determine intent.
- `def run_interactive_alignment(text)`
  Runs the AI-facing algorithm to parse, align, and confirm targets.

---

## `perplexity-inbox/harness/test_cove_ingestion.py`
Verification Test for Cove Ingestion Pipeline.

Connects to the cove database, applies the trial schema, runs a mock
ingestion cycle (insert raw log -> generate vector -> insert node),
and asserts successful database writes.

### Functions
- `def apply_schema(conn)`
- `def run_ingestion_cycle(conn)`
- `def main()`

---

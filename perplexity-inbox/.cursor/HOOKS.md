# Hooks — SSOT + runtime

**Repo SSOT:** `../../.cursor/hooks.json` (constitutional + session chain). Live install: `~/.cursor/hooks.json`.

Do not re-register hooks here — empty `hooks.json` avoids double-firing sessionStart in this workspace.
